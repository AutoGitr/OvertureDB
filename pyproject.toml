[project]
name = "overturedb"
version = "0.1.0"
description = "Curated dataset of clean artwork, verified themes, and franchise sets"
readme = "README.md"
requires-python = "==3.14.7"
dependencies = [
    "jsonschema==4.26.0",
]

[dependency-groups]
dev = [
    "ruff==0.16.7",
]
